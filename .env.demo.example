# Serles ACME Demo - Environment Configuration
#
# Copy to .env.demo and customize:
#   cp .env.demo.example .env.demo
#
# Usage:
#   docker-compose -f docker-compose.demo.yml --env-file .env.demo up -d
#

# Domain name for the demo
# For local testing: demo.serles.local (add to /etc/hosts)
# For production: your-domain.com
DEMO_DOMAIN=demo.serles.local

# Serles ACME server endpoint (via localhost proxy workaround)
# The demo uses ncat proxy to bypass Caddy's HTTPS requirement
# DO NOT change this unless you know what you're doing
ACME_SERVER=http://127.0.0.1:4000/directory
